spring:
  datasource:
    url: jdbc:mysql://localhost:3307/post?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mk123654
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      enabled: false
  application:
    name: auth-service

